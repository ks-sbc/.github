# Obsidian user-specific files
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/workspaces/
.obsidian/publish.json
.obsidian/hotkeys.json
# .obsidian/config - This is the main vault config, often committed, but can be user-specific too.
# For DRUIDS, we will likely manage critical parts of 'config' through specific json files.
# Let's ignore 'config' for now to avoid committing user window positions etc.
# If specific settings from it are needed for all users, they should be part of the planned JSON configs.
.obsidian/config 
.obsidian/star.json
.obsidian/bookmarks.json
.obsidian/local-graph.json
.obsidian/.recycle/ # If recycle bin plugin is used

# Vault trash folder (if configured in Obsidian, common pattern)
.trash/
*.trash

# Common OS-specific files
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment variables file
.env
.env*.local
.env.*.local
